spring:
  #配置数据源(固定配置不要改）
  datasource:
    #配置数据源类型（可有）
    type: com.zaxxer.hikari.HikariDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 123456
    url: jdbc:mysql://localhost:3306/online_exams?characterEncoding=utf-8&useSSL=false


server:
  port: 80


#mybatis-plus配置
mybatis-plus:
  configuration:
    #配置日志
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl



image:
  path: E:\examsImg\